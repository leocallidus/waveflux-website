---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"div"> {
  as?: "div" | "section";
}

const { as: Tag = "div", class: className, ...attrs } = Astro.props;
---

<Tag class:list={["wf-container", className]} {...attrs}>
  <slot />
</Tag>
